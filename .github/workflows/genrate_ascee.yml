name: Generate ASCII Artwork

on:
  push:

jobs:
  Build:
    runs-on: ubuntu-latest

    steps:
      - name: install cowsay program
        run: sudo apt-get install cowsay -y

      - name: Exicute Cowsay CMD
        run: cowsay -f dragon "Run for cover,i am a DRAGON... RAWR">>dragon.txtltra 
      - name: Sleem for 30 seconds 
        run: sleep 30
          
  Test:
    needs: Build
    runs-on: ubuntu-latest

    steps:
      - name: sleep for 10 seconds
        run: sleep 10

      - name: test file exists
        run: grep -i "dragon" dragon.txt
      
        
  Deploy:
    needs: Test
    runs-on: ubuntu-latest

    steps:
      - name: Read File
        run: cat dragon.txt

      - name: deploy
        run: echo Doploying ... ... .
      
